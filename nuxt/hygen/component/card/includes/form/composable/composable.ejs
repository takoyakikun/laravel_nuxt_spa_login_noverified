import { reactive, onMounted } from '@nuxtjs/composition-api'

export default () => {
  const state = reactive({
    formValue: {},
    validate: {}
  })

  onMounted(async () => {
  })

  return { state }
}
